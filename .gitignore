# 编译产物
bin/
obj/
target/
*.user

# 依赖
node_modules/

# ─────────────────────────────────────────────────────────────
# 第三方插件：本地装着、但不是本仓库的作品，不进版本库。
# 注意：不要移动这个目录 —— 宿主 profile 用 link: 指向它，一移动链接就断。
packages/dsh-usage-meter/
# ─────────────────────────────────────────────────────────────

# 本机升级记录（含本机绝对路径），不进公开仓库
docs/

# 本地文件
*.log
.DS_Store
Thumbs.db

# 编辑器
.vs/
.vscode/
.idea/
